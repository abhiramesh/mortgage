		<div class="extra-content-4">
				<div class="form">
					<%= form_for @user, :method => :put, :html => {:id => "form-4" } do |f| %>
						<div class="row">
							<div class="span2">
								<%= f.label :home_value %>
								<%= f.text_field :home_value, placeholder: "Home Value", :style => "width:120px;" %>
								<div class="message" id="home_value_error"></div>
							</div>
							<div class="span2">
								<%= f.label :mortgage_balance %>
								<%= f.text_field :mortgage_balance, placeholder: "Mortgage Balance", :style => "width:120px;" %>
								<div class="message" id="mortgage_balance_error"></div>
							</div>
						</div>
					 		<%= f.submit "Calculate Quote", :class => "submit-4", :id => "submit-part-4" %>
						<% end %>
						<div class="back-link" id="back-link-3">Back</div>
				</div>
			</div>
			<script type="text/javascript">
			$('input#user_mortgage_balance').blur(function(event){
				if ($('input#user_mortgage_balance').val().length < 1) {
					event.preventDefault();
					$('#mortgage_balance_error').empty().append("Enter a correct mortgage balance");
				}
				if ($('input#user_mortgage_balance').val().length > 0) {
					$('#mortgage_balance_error').empty();
				}
			});
			$('input#user_home_value').blur(function(event){
				if ($('input#user_home_value').val().length < 1) {
					event.preventDefault();
					$('#home_value_error').empty().append("Enter a correct home value");
				}
				if ($('input#user_home_value').val().length > 0) {
					$('#home_value_error').empty();
				}
			});
			$('#submit-part-4').click(function(event){
				if ($('input#user_mortgage_balance').val().length < 1) {
					event.preventDefault();
					$('#mortgage_balance_error').empty().append("Enter a correct mortgage balance");
				}
				if ($('input#user_home_value').val().length < 1) {
					event.preventDefault();
					$('#home_value_error').empty().append("Enter a correct home value");
				}
			});
			$('#back-link-3').click(function(){
					$('.extra-content-4').hide();
					$('.extra-content-3').show().insertAfter('div.ribbon-content');
			});
			</script>
	